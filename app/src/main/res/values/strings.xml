<resources>
    <string name="app_name">DryNav</string>
    <!-- mapbox_access_token itself is generated by app/build.gradle.kts (resValue)
         from local.properties' MAPBOX_ACCESS_TOKEN — never hardcoded here,
         so a real personal token never ends up in git. -->

</resources>
