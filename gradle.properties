org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# --- SECRETS (do not commit real values) ---
# Get this token from https://account.mapbox.com/ -> Tokens -> create a token
# with the DOWNLOADS:READ secret scope. Put the REAL value in your own
# ~/.gradle/gradle.properties (machine-global, never part of this repo) —
# not here.
MAPBOX_DOWNLOADS_TOKEN=
